<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FinalProject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
    </style>
    
    <style name="NumberPickerStyle" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/accentWhite</item>
        <!-- Remove divider -->
        <item name="colorControlNormal">@color/lightTheme</item>
        <item name="android:textColorPrimary">@color/accentYellow</item>
        <item name="android:background">@color/darkTheme</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="SwitchStyle" parent="Theme.AppCompat.Light">
        <item name="android:textSize">24sp</item>
        <item name="android:padding">24dp</item>
        <item name="colorControlActivated">@color/accentYellow</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/accentWhite
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/lightTheme
        </item>
    </style>
</resources>